import { takeLatest, all } from "redux-saga/effects";
// import { FORTEST } from "../action.types";

// import { onTest } from "./fortest.saga";

function* sagas() {
  // yield all([takeLatest(FORTEST, onTest)]);
}

export default sagas;
